{
  "name": "genisys",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0 <23.0.0"
  },
  "devEngines": {
    "runtime": {
      "name": "bun",
      "version": ">=1.2.0"
    }
  },
  "packageManager": "pnpm@10.20.0",
  "scripts": {
    "dev": "just dev",
    "build": "just build",
    "test": "vitest run --config src/apps/api/vitest.config.ts && vitest run --config src/libs/shared/vitest.config.ts && vitest run --config src/libs/logger/vitest.config.ts",
    "test:unit": "pnpm test",
    "test:web": "pnpm --filter @repo/web exec vitest run",
    "test:e2e": "just test-e2e",
    "lint": "eslint .",
    "typecheck": "pnpm --filter @repo/shared typecheck && pnpm --filter @repo/logger typecheck && pnpm --filter @repo/web typecheck",
    "typecheck:api": "pnpm --filter @repo/api typecheck",
    "start": "node src/apps/api/dist/index.js"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "@parcel/watcher",
      "better-sqlite3"
    ]
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@vitest/coverage-v8": "catalog:",
    "eslint": "catalog:",
    "tsup": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
